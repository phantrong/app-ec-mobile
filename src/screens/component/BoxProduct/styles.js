import { StyleSheet } from 'react-native';

const styles = StyleSheet.create({
    wrapper: {
        width: 200,
        marginHorizontal: 10,
        marginVertical: 10,
        borderColor: 'black',
        borderWidth: 1,
    },
});

export default styles;
