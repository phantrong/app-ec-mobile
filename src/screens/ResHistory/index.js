import { View, Text } from 'react-native';
import React from 'react';

const ResHistory = () => {
    return (
        <View>
            <Text>ResHistory</Text>
        </View>
    );
};

export default ResHistory;
